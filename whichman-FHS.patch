diff -urN whichman-1.9/Makefile whichman-1.9-new/Makefile
--- whichman-1.9/Makefile	Tue Apr 11 22:04:09 2000
+++ whichman-1.9-new/Makefile	Thu Mar  8 21:40:03 2001
@@ -33,15 +33,12 @@
 	strip ftwhich
 	strip ftff
 	[ -d "$(PREFIX)/bin" ] || $(INSTALL) -d $(PREFIX)/bin
-	[ -d "$(PREFIX)/man/man1" ] || $(INSTALL) -d $(PREFIX)/man/man1
+	[ -d "$(PREFIX)/share/man/man1" ] || $(INSTALL) -d $(PREFIX)/share/man/man1
 	$(INSTALL) -m 755 whichman $(PREFIX)/bin
 	$(INSTALL) -m 755 ftwhich $(PREFIX)/bin
 	$(INSTALL) -m 755 ftff $(PREFIX)/bin
-	for p in $(MANP) ; do \
-		echo "installing $$p in $(PREFIX)/man/man1"; \
-		$(INSTALL) -m 644 $$p $(PREFIX)/man/man1 ;\
-	done
-
+	$(INSTALL) $(MANP) -m 644 $(PREFIX)/share/man/man1
+	
 install_with_cp: whichman ftff ftwhich $(MANP)
 	chmod 755 whichman ftff ftwhich
 	[ -d "$(PREFIX)/bin" ] || mkdir -p $(PREFIX)/bin
diff -urN whichman-1.9/whichman.c whichman-1.9-new/whichman.c
--- whichman-1.9/whichman.c	Wed May 10 22:07:25 2000
+++ whichman-1.9-new/whichman.c	Thu Mar  8 21:40:58 2001
@@ -399,8 +399,8 @@
 
 	mpath = (char *) getenv("MANPATH");
 	if (mpath == NULL || *mpath == '\0') {
-		fprintf(stderr,"Warning: MANPATH not defined, using /usr/man\n");
-                mpath="/usr/man";
+		fprintf(stderr,"Warning: MANPATH not defined, using /usr/share/man\n");
+                mpath="/usr/share/man";
 	}
         /* make a copy that is static */
         cmpath = (char *)malloc(sizeof(char)*(strlen(mpath) + 2));
